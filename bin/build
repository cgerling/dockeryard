#!/bin/bash

source "$(dirname $0)/args"

DOCKERFILE="$BASE_PATH/Dockerfile"

BUILD_ARGS="--force-rm"
BUILD_CMD="docker build --tag $TAG --file $DOCKERFILE $BUILD_ARGS $CMD_ARGS $BASE_PATH"

eval $BUILD_CMD

