#!/bin/bash

source "$(dirname $0)/default_values"

IFS=':' read -ra ARG_VALUES <<< "$1"

IMAGE=${ARG_VALUES[0]}
VERSION=${ARG_VALUES[1]}

shift

TAG="$REGISTRY/$USER/$REPOSITORY/$IMAGE:$VERSION"
CMD_ARGS=$@
